<% unless @tenant_image.nil? %>
  $("#edit-image").html("<%= j(raw( render partial: 'modal_form', locals: {tenant_image: @tenant_image, title: "Edit", url: admin_tenant_image_path(@tenant.id, @tenant_image.id), method: 'put'}  )) %>");
  $("#edit-image-modal").modal("show")
<% end %>
