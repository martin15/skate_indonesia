<% unless @hotel_image.nil? %>
  $("#edit-image").html("<%= j(raw( render partial: 'modal_form', locals: {hotel_image: @hotel_image, title: "Edit", url: admin_hotel_image_path(@hotel.id, @hotel_image.id), method: 'put'}  )) %>");
  $("#edit-image-modal").modal("show")
<% end %>
